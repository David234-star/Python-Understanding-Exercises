def calculation(a, b):  # defining the function name as calculation
    add = a + b  # addition of two numbers
    sub = a - b  # subtraction of two numbers
    return add, sub  # returning the addition and subtraction to the function call


result = calculation(40, 10)  # function calling to the variable called result
print(result)  # result of the addition and subtraction in output form
